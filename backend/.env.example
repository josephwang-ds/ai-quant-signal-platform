# 逗号分隔的前端来源，用于 CORS（Render / 生产环境必填）
# 示例：https://your-app.vercel.app,https://www.your-domain.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Supabase Postgres connection string for durable research assets.
# Use the Transaction Pooler URI from Supabase Connect.
# Do not commit real credentials.
SUPABASE_DB_URL=

# Render 会自动注入 PORT；本地开发可省略
# PORT=8000
