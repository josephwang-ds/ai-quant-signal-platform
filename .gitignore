__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
.DS_Store

# Local provider secrets. Commit only the empty example template.
.env
.env.*
!.env.example

# Data: cache and derived artifacts are reproducible, never committed.
# data/sample/ IS committed — it is what makes `make demo` work offline.
data/cache/
data/build/
build/
dist/
*.parquet
!data/sample/*.parquet
